[project]
name = "image-processor"
version = "1.0.0"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "aws-cdk-lib>=2.265.0",
    "constructs>=10.8.1",
    "pre-commit>=4.6.1",
    "ruff>=0.11.10",
]
