[project]
name = "image-processor"
version = "0.1.2"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "aws-cdk-lib>=2.265.0",
    "constructs>=10.8.1",
    "pre-commit>=4.6.1",
    "pytest>=8.3.0",
    "ruff>=0.11.10",
]
